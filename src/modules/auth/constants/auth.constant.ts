/** Request key used to store the decoded authenticated user payload on Express request objects. */
export const REQUEST_USER_KEY = 'user';

/** Metadata key used by the @Auth() decorator to define route-level authentication strategy. */
export const AUTH_TYPE_KEY = 'authType';